events {
        worker_connections 1024;
}

http {
        server {
                listen 80;
                server_name testdomain.com;

                root /www;
                index index.html;
        }
}